= render 'signup/shared/signup-head'
.single-container
  %header.single-header
    %h1
      = link_to "/" do
        = image_tag "mercari_logo.png", class: 'single-header__logo'
    %nav.progress
      %ul.progress__bar
        %li.active
          会員情報
          .progress-status.active
          .progress-status-line.active
        %li.active
          電話番号認証
          .progress-status.active
          .progress-status-line
        %li
          お届け先住所入力
          .progress-status
          .progress-status-line
        %li.payment
          支払い方法
          .progress-status
          .progress-status-line
        %li.complete
          完了
          .progress-status


  .single-main
    .new-registration
      .new-registration-container
        .new-registration-container__header
          %h2.new-registration-container__header--word 電話番号の確認
        .new-registration-container__body
          -# 入力form_for
          .new-registration-body-upper
            = form_with model: @user, url: address_signup_index_path, local: true, method: :get, class: 'new-registration-field' do |f|
              .new-registration-field__group
                = f.label :携帯電話の番号, class: 'new-registration-field__group--name'
                = f.text_field :nickname, class: 'new-registration-field__group--input-default', placeholder: '携帯電話の番号を入力'
                %p.new-registration-field__group--single-text
                  本人確認のため、携帯電話のSMS(ショートメッセージサービス)を利用して認証を行います。
              = form_with model: @user, url: address_signup_index_path, local: true, method: :get, class: 'new-registration-field' do |f|
                = f.submit 'SMSを送信する',class: "btn-default-red"
                %p.new-registration-field__group--single-text
                  ※電話番号は本人確認や不正利用防止のために利用します。他のユーザーに公開されることはありません。
                .reason-confirm-phone-number
                  %p
                    = link_to '電話番号の確認が必要な理由', "#"
                    %i.icon-arrow-right.fas.fa-chevron-right

= render 'signup/shared/signup-footer'